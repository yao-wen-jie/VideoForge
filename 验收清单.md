# 视频自动化 VideoForge — 用户验收检查清单

> 验收目标：确认 Skill 包从任意目录解压后，3分钟内可正常运行，核心功能可用。

---

## 一、安装验收（必须在3分钟内完成）

### ✅ 检查项 1.1：文件完整性
**操作**：解压后检查以下文件是否存在

```
skill/
├── app.py                    ✅ 必须存在
├── start.bat                 ✅ 必须存在
├── requirements.txt          ✅ 必须存在
├── README.md                 ✅ 必须存在
├── modules/                  ✅ 必须存在（含2个.py文件）
├── templates/index.html      ✅ 必须存在
├── static/js/app.js         ✅ 必须存在
└── video-shotcraft/api/library.json  ✅ 必须存在
```

**通过标准**：以上文件全部存在 → ✅ 通过

---

### ✅ 检查项 1.2：一键启动
**操作**：双击 `start.bat`

**预期结果**：
1. 弹出黑色命令行窗口
2. 显示 "检测 Python 环境..."
3. 显示 "安装依赖..."（首次）
4. 显示 "🎬 视频自动化 Web 控制台启动中..."
5. 浏览器自动打开 `http://localhost:5000`

**通过标准**：浏览器成功打开控制台首页 → ✅ 通过

---

### ✅ 检查项 1.3：无 config.json 也能启动
**操作**：
```bash
# 1. 确保没有 config.json
ls config.json   # 应该不存在

# 2. 启动
python app.py
```

**预期结果**：应用正常启动，路径自动基于当前目录计算

**通过标准**：不报错、不闪退 → ✅ 通过

---

## 二、功能验收（逐个验证）

### ✅ 检查项 2.1：选题矩阵
**操作**：
1. 浏览器打开 `http://localhost:5000`
2. 点击左侧导航「📋 选题矩阵」
3. 选择分类（如"情感"）
4. 点击「生成选题」

**预期结果**：
- 页面显示 10 条选题
- 每条有标题、钩子、目标人群、标签
- 无报错

**通过标准**：成功生成 10 条选题 → ✅ 通过

---

### ✅ 检查项 2.2：AI脚本生成（需配置 DeepSeek Key）
**操作**：
1. 进入「✍️ AI脚本」页面
2. 输入选题："为什么越懂事的人越容易被忽视？"
3. 选择风格："口播"
4. 点击「生成脚本」

**预期结果（已配置 Key）**：
- 显示完整脚本（含开场/痛点/转折/金句/CTA）
- 标注每个段落时长

**预期结果（未配置 Key）**：
- 提示"未配置 DeepSeek API Key"
- 显示配置指引

**通过标准**：有明确响应（成功或提示配置）→ ✅ 通过

---

### ✅ 检查项 2.3：镜头卡库
**操作**：
1. 进入「🎬 镜头卡库」
2. 等待页面加载

**预期结果**：
- 显示 "104 张镜头配方卡"
- 显示分类列表（品牌开场、转场特效等）
- 可搜索关键词（如"开场"）

**通过标准**：显示镜头卡列表 → ✅ 通过

---

### ✅ 检查项 2.4：成本追踪
**操作**：
1. 进入「💰 成本追踪」
2. 查看今日/本周/本月汇总

**预期结果**：
- 显示成本表格（时间/服务/操作/消耗/状态）
- 显示预算使用率

**通过标准**：页面正常显示 → ✅ 通过

---

### ✅ 检查项 2.5：系统设置
**操作**：
1. 点击「⚙️ 系统设置」
2. 查看各项配置

**预期结果**：
- API Keys 区域显示可输入框
- 路径配置显示自动计算的相对路径
- 无 `C:/Users/XXX/Desktop/视频自动化1.1` 等旧路径

**通过标准**：路径正确、无旧硬编码路径 → ✅ 通过

---

## 三、工作流脚本验收

### ✅ 检查项 3.1：脚本列表
**操作**：
1. 进入「🔧 工作流」页面
2. 查看可用脚本列表

**预期结果**：
- 显示 10 个脚本（环境检查、每日选题、视频生成等）
- 部分脚本显示「占位」标签

**通过标准**：列表加载成功 → ✅ 通过

---

### ✅ 检查项 3.2：运行环境检查脚本
**操作**：
1. 在工作流页面点击「环境检查」
2. 点击「运行」

**预期结果**：
- 任务状态变为「运行中」
- 输出区域显示 Python 版本、Flask 版本、FFmpeg 状态等
- 最终状态变为「已完成」

**通过标准**：脚本成功执行并输出诊断信息 → ✅ 通过

---

## 四、跨机器兼容性验收

### ✅ 检查项 4.1：路径动态计算
**操作**：
```bash
# 1. 将整个 skill 文件夹复制到另一个位置
# 如：D:/test/VideoForge/

# 2. 从新的位置启动
python app.py

# 3. 查看系统设置中的路径
```

**预期结果**：
- workflow_root 显示新位置的 workspace/ 路径
- 不是旧路径 `C:/Users/永恒的心/Desktop/...`

**通过标准**：路径随目录变化 → ✅ 通过

---

### ✅ 检查项 4.2：删除 config.json 后重启
**操作**：
```bash
# 1. 如果存在 config.json，删除它
rm config.json

# 2. 重启应用
python app.py
```

**预期结果**：
- 应用正常启动
- 不报错
- 自动生成新的默认配置

**通过标准**：无 config.json 也能正常启动 → ✅ 通过

---

## 五、文档验收

### ✅ 检查项 5.1：README 完整性
**操作**：打开 `README.md`

**预期内容**：
- [x] 项目名称和一句话介绍
- [x] 功能全景表
- [x] 截图展示（5张）
- [x] 快速开始步骤
- [x] API Key 配置说明
- [x] 项目结构图
- [x] API 列表

**通过标准**：以上内容齐全 → ✅ 通过

---

### ✅ 检查项 5.2：使用说明可用性
**操作**：打开 `使用说明.md`

**预期内容**：
- [x] 系统要求
- [x] 安装步骤（一键/手动）
- [x] 首次配置指引
- [x] 核心功能使用流程
- [x] 常见问题解答（≥5个）

**通过标准**：新用户可按文档独立完成安装 → ✅ 通过

---

## 六、GitHub 发布前检查

### ✅ 检查项 6.1：.gitignore
**操作**：打开 `.gitignore`

**预期内容包含**：
```
config.json
__pycache__/
*.pyc
.venv/
workspace/
```

**通过标准**：API Keys 和用户数据不会被提交 → ✅ 通过

---

### ✅ 检查项 6.2：LICENSE
**操作**：打开 `LICENSE`

**预期结果**：包含 MIT 许可证全文

**通过标准**：有明确开源协议 → ✅ 通过

---

## 验收结论模板

完成以上检查项后，填写：

```
验收日期：____年__月__日
验收人：______________

总检查项：__项
通过：__项
失败：__项
未测试：__项

结论：
□ 通过 — 可以发布到 GitHub
□ 有条件通过 — 需修复 __ 项后发布
□ 不通过 — 需修复后重新验收

备注：______________________________
```

---

*验收清单版本：v1.0 | 适用版本：VideoForge 2.0*
